// Re-export adapter-related types from main types
export type {
  FeedbackAdapter,
  FeedbackAdapterResult,
  FeedbackPayload,
  FeedbackUser,
  FeedbackMetadata,
  FeedbackScreenshot,
} from '../types'
