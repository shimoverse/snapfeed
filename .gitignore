node_modules/
dist/
*.log
.DS_Store
.env
.env.local

# Docker stack runtime data — but keep the .gitkeep placeholders so the dir
# exists in fresh clones with the right ownership for bind mounts (see
# docker/data/.gitkeep for the rationale).
docker/data/*
!docker/data/.gitkeep
!docker/data/audit/
!docker/data/audit/.gitkeep
!docker/data/uploads/
!docker/data/uploads/.gitkeep
docker/data/audit/*
!docker/data/audit/.gitkeep
docker/data/uploads/*
!docker/data/uploads/.gitkeep
docker/.env

# TypeScript incremental build info
*.tsbuildinfo

# Audit + uploads written by file adapter / file storage / file audit log
feedback.jsonl
snapfeed-audit.jsonl
snapfeed-uploads/
