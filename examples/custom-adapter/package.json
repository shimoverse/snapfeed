{
  "name": "snapfeed-example-custom-adapter",
  "private": true,
  "version": "0.0.0",
  "description": "Example: writing your own snapfeed adapter for a destination snapfeed doesn't ship out of the box. Uses Mattermost (Slack alternative) as the worked example, but the same pattern applies to any HTTP-based system.",
  "type": "module",
  "scripts": {
    "type-check": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "snapfeed": "file:../.."
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
