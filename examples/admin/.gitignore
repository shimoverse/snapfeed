node_modules
.next
.env
.env.local

# Auto-generated by Next.js / tsc — regenerated on every build.
next-env.d.ts
*.tsbuildinfo

# Local feedback / audit data — these contain real user submissions (text,
# screenshots, console errors, build SHAs, etc.). NEVER commit them.
*.jsonl
feedback.json
snapfeed-audit.json
